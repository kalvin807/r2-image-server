{
  "name": "r2-image-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @r2-image-server/api dev",
    "start": "pnpm --filter @r2-image-server/api start",
    "db:generate": "pnpm --filter @r2-image-server/api db:generate",
    "db:migrate": "pnpm --filter @r2-image-server/api db:migrate",
    "db:push": "pnpm --filter @r2-image-server/api db:push",
    "db:studio": "pnpm --filter @r2-image-server/api db:studio"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
